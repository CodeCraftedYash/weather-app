import HouseWithGround from "../assets/houseWithGround.png";

const Ground = () => (
  <div className="scale-120 origin-bottom-right max-sm:scale-[100%] sm:scale-[70%]">
    <img src={HouseWithGround} alt="House with ground" />
  </div>
);

export default Ground;
